export type serviceType = {
  title: string;
  description: string;
  imageSrc: string;
};

export type heroDataType = {
  title: string;
  subtitle: string;
  imageSrc: string;
};

export type productType = {
  title: string;
  description: string;
  imageSrc: string;
};
